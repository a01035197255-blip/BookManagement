spring:
  datasource:
    url: jdbc:mysql://awseb-e-ppzmaadqdf-stack-awsebrdsdatabase-37eyl5iugvt0.cbkkm66u0z6l.ap-northeast-2.rds.amazonaws.com:3306/bookdb
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.example.bookmanagement: DEBUG